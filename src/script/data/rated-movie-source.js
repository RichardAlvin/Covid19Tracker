class RatedMovieSource {
  static getAllRatedMovie() {
    return new Promise((resolve, reject) => {
      // const filteredFavorites = favorites.filter(favorite => favorite.name.toUpperCase().includes(keyword.toUpperCase()));
      
      // if (filteredFavorites.length) {
      //   resolve(filteredFavorites);
      // } else {
      //   reject(`${keyword} is not found`);
      // }
    });
  }
}

export default RatedMovieSource;